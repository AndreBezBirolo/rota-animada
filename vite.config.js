import { defineConfig } from 'vite';

export default defineConfig({
  // Aponta para o nome do repositório no GitHub para que os assets
  // sejam servidos corretamente via GitHub Pages
  base: '/rota-animada/',
});
